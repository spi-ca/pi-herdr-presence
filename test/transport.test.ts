import {afterEach,expect,test} from "bun:test";import * as fs from "node:fs/promises";import * as os from "node:os";import {join} from "node:path";import {safeSocketFingerprint} from "../src/identity.js";import {HerdrSocketTransport} from "../src/transport.js";import {fakeSocket} from "./helpers/fake-socket.js";
const clean:Array<()=>Promise<void>>=[];afterEach(async()=>{while(clean.length)await clean.pop()!();});
test("uses one bounded request/response exchange and fails closed on unsafe socket",async()=>{const dir=await fs.mkdtemp(join(os.tmpdir(),"herdr-presence-"));const path=join(dir,"socket");const server=await fakeSocket(path,()=>'{"id":"x","result":{"type":"ok"}}');clean.push(async()=>{await server.close();await fs.rm(dir,{recursive:true,force:true});});const transport=new HerdrSocketTransport(path,100,2);await expect(transport.request("x\n")).resolves.toContain('"id":"x"');await transport.close();});
test("accepts a valid owner-only socket",async()=>{const dir=await fs.mkdtemp(join(os.tmpdir(),"herdr-fingerprint-"));const path=join(dir,"socket");const server=await fakeSocket(path,()=>"ok");clean.push(async()=>{await server.close();await fs.rm(dir,{recursive:true,force:true});});const entry=await fs.lstat(path);await expect(safeSocketFingerprint(path)).resolves.toEqual({dev:entry.dev,ino:entry.ino,uid:entry.uid});});
test("rejects ambiguous lexical socket traversal",async()=>{const dir=await fs.mkdtemp(join(os.tmpdir(),"herdr-ambiguous-"));const path=join(dir,"socket");const nested=join(dir,"nested");const server=await fakeSocket(path,()=>"ok");await fs.mkdir(nested);clean.push(async()=>{await server.close();await fs.rm(dir,{recursive:true,force:true});});await expect(safeSocketFingerprint(`${nested}/../socket`)).rejects.toThrow("ambiguous traversal");});
test("rejects unsafe lexical symlink ancestors",async()=>{const outer=await fs.mkdtemp(join(os.tmpdir(),"herdr-symlink-"));const target=join(outer,"target");const link=join(outer,"link");await fs.mkdir(target,{mode:0o700});await fs.symlink(target,link);const server=await fakeSocket(join(target,"socket"),()=>"ok");clean.push(async()=>{await server.close();await fs.rm(outer,{recursive:true,force:true});});await expect(safeSocketFingerprint(join(link,"socket"))).rejects.toThrow("symlink");});
test("checks the complete resolved ancestor chain",async()=>{const outer=await fs.mkdtemp(join(os.tmpdir(),"herdr-unsafe-"));const inner=join(outer,"private");await fs.mkdir(inner,{mode:0o700});const server=await fakeSocket(join(inner,"socket"),()=>"ok");clean.push(async()=>{await server.close();await fs.rm(outer,{recursive:true,force:true});});await fs.chmod(outer,0o777);await expect(safeSocketFingerprint(join(inner,"socket"))).rejects.toThrow("replaceable");});
test("priority release displaces saturated pending work even at maxQueue=1",async()=>{const {BoundedSocketQueue}=await import("../src/transport.js");const queue=new BoundedSocketQueue(1);let release!:()=>void;const started:string[]=[];const active=queue.enqueue(async()=>{started.push("active");await new Promise<void>(resolve=>{release=resolve;});return "active";});await new Promise(resolve=>setTimeout(resolve,0));const pending=queue.enqueue(async()=>{started.push("pending");return "pending";},"state");const priority=queue.enqueue(async()=>{started.push("release");return "release";},"release",true);await expect(pending).rejects.toThrow("priority release");release();await expect(active).resolves.toBe("active");await expect(priority).resolves.toBe("release");expect(started).toEqual(["active","release"]);await queue.close(10);});
test("keyed writes settle superseded callers and close aborts without dispatching queued work",async()=>{const {BoundedSocketQueue}=await import("../src/transport.js");const queue=new BoundedSocketQueue(2);let started=false;const active=queue.enqueue(signal=>new Promise<string>((_resolve,reject)=>{started=true;signal.addEventListener("abort",()=>reject(new Error("aborted")),{once:true});}));void active.catch(()=>{});await new Promise(resolve=>setTimeout(resolve,0));const first=queue.enqueue(async()=>"old","state");void first.catch(()=>{});const latest=queue.enqueue(async()=>"latest","state");void latest.catch(()=>{});expect(latest).not.toBe(first);const queued=queue.enqueue(async()=>"never","other");void queued.catch(()=>{});await queue.close(20);expect(started).toBe(true);await expect(active).rejects.toThrow("closed during active work");await expect(first).rejects.toThrow("coalesced");await expect(latest).rejects.toThrow("closed before dispatch");await expect(queued).rejects.toThrow("closed before dispatch");});
test("immediate close does not wait for abort-unaware active work",async()=>{const {BoundedSocketQueue}=await import("../src/transport.js");const queue=new BoundedSocketQueue(1);let started!:()=>void;let finish!:(value:string)=>void;const began=new Promise<void>(resolve=>{started=resolve;});const work=new Promise<string>(resolve=>{finish=resolve;});const active=queue.enqueue(async()=>{started();return await work;});void active.catch(()=>{});await began;await expect(Promise.race([queue.close(0).then(()=>"closed"),new Promise<string>(resolve=>setTimeout(()=>resolve("timed out"),50))])).resolves.toBe("closed");await expect(active).rejects.toThrow("closed during active work");finish("late");await new Promise(resolve=>setTimeout(resolve,0));});
