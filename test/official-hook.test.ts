import {expect,test} from "bun:test";import * as fs from "node:fs/promises";import * as os from "node:os";import {join} from "node:path";import {officialHookDetected,officialHookStatus} from "../src/official-hook.js";
test("only an absent managed file permits the local integration",async()=>{const root=await fs.mkdtemp(join(os.tmpdir(),"herdr-managed-"));try{await fs.mkdir(join(root,"extensions"));const managed=join(root,"extensions","herdr-agent-state.ts");await fs.writeFile(managed,"// HERDR_INTEGRATION_ID=pi\n");expect(await officialHookStatus({PI_CODING_AGENT_DIR:root})).toBe("present");expect(await officialHookDetected({PI_CODING_AGENT_DIR:root})).toBe(true);await fs.writeFile(managed,"// managed asset without the expected marker\n");expect(await officialHookStatus({PI_CODING_AGENT_DIR:root})).toBe("unknown");expect(await officialHookDetected({PI_CODING_AGENT_DIR:root})).toBe(true);expect(await officialHookStatus({PI_CODING_AGENT_DIR:join(root,"missing")})).toBe("absent");await fs.rm(managed);expect(await officialHookStatus({PI_CODING_AGENT_DIR:root})).toBe("absent");await fs.mkdir(managed);expect(await officialHookStatus({PI_CODING_AGENT_DIR:root})).toBe("unknown");expect(await officialHookDetected({PI_CODING_AGENT_DIR:root})).toBe(true);}finally{await fs.rm(root,{recursive:true,force:true});}});
