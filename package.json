{
  "name": "pi-herdr-presence",
  "version": "0.1.0",
  "main": "index.ts",
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@earendil-works/pi-coding-agent": "^0.82.0",
    "@types/node": "^25.2.3",
    "bun-types": "^1.3.14",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.82.0"
  },
  "description": "Socket-only Herdr presence observer for Pi TUI sessions",
  "files": ["index.ts", "src/**/*", "docs/**/*.md", "AGENTS.md", "README.md", "CHANGELOG.md", "LICENSE"],
  "keywords": ["pi-package", "herdr", "presence"],
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "private": true,
  "scripts": {
    "test": "bun test --pass-with-no-tests",
    "check": "bunx tsc --noEmit",
    "lint": "biome lint --error-on-warnings .",
    "ci": "bun run lint && bun run check && bun run test"
  },
  "type": "module",
  "dependencies": {
    "@pi/presence": "github:spi-ca/pi-presence#v2-20260818-2"
  }
}
