{
  "name": "pi-herdr-presence",
  "version": "0.1.0",
  "main": "index.ts",
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@types/node": "^25.2.3",
    "bun-types": "^1.3.14",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "description": "Socket-only Herdr presence observer for Pi TUI sessions",
  "files": ["index.ts", "src/**/*", "scripts/**/*.ts", "docs/**/*.md", "AGENTS.md", "README.md", "CHANGELOG.md", "LICENSE"],
  "keywords": ["pi-package", "herdr", "presence"],
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "private": true,
  "scripts": {
    "test": "bun test",
    "coverage": "bun test --coverage --coverage-reporter=text",
    "coverage:check": "bun scripts/check-coverage.ts",
    "check": "bunx tsc --noEmit",
    "lint": "biome lint --error-on-warnings .",
    "smoke:live": "bun scripts/live-herdr-presence-producers.ts",
    "ci": "bun run lint && bun run check && bun run coverage:check"
  },
  "type": "module",
  "dependencies": {
    "@pi/presence": "github:spi-ca/pi-presence#v2-20260818-2"
  }
}
